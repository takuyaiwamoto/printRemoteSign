// 送信側（ブラウザ）のデフォルト接続先
// このファイルを編集すれば、URLパラメータ無しで動作します。
// 例: window.SERVER_URL を Render の URL に、window.CHANNEL を任意名に。

window.SERVER_URL = 'wss://printremotesign.onrender.com';
window.CHANNEL = 'default';

